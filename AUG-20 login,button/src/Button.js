import React from 'react';

const LoginButton = () => {
  return (
    <button>Login</button>
  );
};

const LogoutButton = () => {
  return (
    <button>Logout</button>
  );
};

export { LoginButton, LogoutButton };
